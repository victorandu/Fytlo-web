# Waitlist SMTP Configuration
# Required for waitlist signup emails

# Email address to receive waitlist signups
WAITLIST_TO_EMAIL=you@example.com

# SMTP server settings (e.g., Gmail, SendGrid, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: From address (defaults to SMTP_USER if not set)
# SMTP_FROM=noreply@fytlo.com
